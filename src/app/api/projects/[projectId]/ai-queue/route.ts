import { ZodError } from "zod";
import { AIFollowUpService } from "@/domain/ai-queue/service";
import { userMessage } from "@/domain/shared/errors";
import { getCurrentUser } from "@/server/auth/session";

export async function GET(request: Request, { params }: { params: Promise<{ projectId: string }> }) { const user = await getCurrentUser(); if (!user) return Response.json({ error: "Authentication required." }, { status: 401 }); try { const { projectId } = await params; const url = new URL(request.url); const type = url.searchParams.get("targetType"); const targetType = type === "page" || type === "building_block" ? type : undefined; return Response.json({ items: await new AIFollowUpService().list(user.id, projectId, targetType, url.searchParams.get("targetId") ?? undefined) }, { headers: { "Cache-Control": "private, no-store" } }); } catch (error) { return Response.json({ error: userMessage(error, "Follow-up instructions could not be loaded.") }, { status: 404 }); } }
export async function POST(request: Request, { params }: { params: Promise<{ projectId: string }> }) { const user = await getCurrentUser(); if (!user) return Response.json({ error: "Authentication required." }, { status: 401 }); try { const { projectId } = await params; const body = await request.json() as object; return Response.json(await new AIFollowUpService().create(user.id, { ...body, projectId }), { status: 201 }); } catch (error) { return Response.json({ error: error instanceof ZodError ? error.issues[0]?.message : userMessage(error, "This follow-up could not be queued.") }, { status: 400 }); } }
